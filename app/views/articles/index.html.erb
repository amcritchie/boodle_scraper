<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <!-- Header -->
  <div class="bg-gradient-to-r from-indigo-600 to-purple-600 rounded-xl p-6 shadow-lg mb-6">
    <div class="flex justify-between items-center">
      <div>
        <h1 class="text-2xl font-bold text-white">Articles</h1>
        <p class="text-indigo-100 text-sm mt-1"><%= @articles.count %> articles</p>
      </div>
      <a href="<%= new_article_path %>" class="bg-white text-indigo-600 hover:bg-indigo-50 px-4 py-2 rounded-lg text-sm font-semibold transition-colors shadow">
        + New Article
      </a>
    </div>
  </div>

  <% if flash[:notice] %>
    <div class="bg-green-100 dark:bg-green-900 border border-green-400 dark:border-green-700 text-green-700 dark:text-green-200 px-4 py-3 rounded-lg mb-6" role="alert">
      <%= flash[:notice] %>
    </div>
  <% end %>

  <!-- Search Filter -->
  <div data-controller="searchable-table">
    <div class="mb-4 relative">
      <input type="text"
             data-searchable-table-target="searchInput"
             placeholder="Filter articles by title, author, name, source..."
             class="w-full px-4 py-2 pl-10 rounded-lg border theme-border-primary theme-bg-primary theme-text-primary text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500"
             autofocus>
      <svg class="absolute left-3 top-2.5 h-4 w-4 theme-text-secondary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
      </svg>
      <button data-searchable-table-target="clearButton" class="absolute right-3 top-2 items-center justify-center h-5 w-5 rounded-full bg-gray-200 dark:bg-gray-600 hover:bg-gray-300 dark:hover:bg-gray-500 text-xs" style="display: none;">&times;</button>
    </div>

  <!-- Article Cards -->
  <div class="space-y-4" data-searchable-table-target="tableBody">
    <% @articles.each do |article| %>
      <div class="theme-bg-primary shadow-lg rounded-xl border theme-border-primary p-5" data-search="<%= [article.id, article.title, article.title_summary, article.author, article.main_person_name, article.main_person_slug, article.main_team_name, article.main_team_slug, article.source, article.source_id, article.sport, article.context, article.model, article.process].compact.join(' ').downcase %>">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-6">

          <!-- Column 1: ID, Title, Author, Dates -->
          <div>
            <p class="text-xs font-mono theme-text-secondary mb-1">#<%= article.id %></p>
            <h2 class="text-lg font-bold theme-text-primary"><%= article.title %></h2>
            <% if article.title_summary.present? %>
              <p class="text-sm theme-text-secondary italic mt-1"><%= article.title_summary %></p>
            <% end %>
            <p class="text-sm theme-text-secondary mt-1"><%= article.author %></p>
            <% if article.sport.present? %>
              <span class="inline-block mt-2 px-2 py-0.5 text-xs font-semibold rounded-full bg-indigo-100 text-indigo-800 dark:bg-indigo-900 dark:text-indigo-200"><%= article.sport.upcase %></span>
            <% end %>
            <p class="text-sm theme-text-secondary mt-2">Published: <span class="theme-text-primary"><%= article.published_at&.strftime("%b %d, %Y") || "—" %></span></p>
            <p class="text-sm theme-text-secondary">Reviewed: <span class="theme-text-primary"><%= article.reviewed_at&.strftime("%b %d, %Y %l:%M %p") || "—" %></span></p>
          </div>

          <!-- Column 2: Team & Athlete Info -->
          <div>
            <p class="text-sm font-medium theme-text-secondary">Primary Team</p>
            <p class="text-sm theme-text-primary"><%= article.main_team_name.presence || "—" %></p>
            <p class="text-xs font-mono theme-text-secondary mb-3"><%= article.main_team_slug %></p>

            <p class="text-sm font-medium theme-text-secondary">Primary Athlete</p>
            <p class="text-sm theme-text-primary"><%= article.main_person_name.presence || "—" %></p>
            <p class="text-xs font-mono theme-text-secondary mb-3"><%= article.main_person_slug %></p>

            <p class="text-sm font-medium theme-text-secondary">Source</p>
            <% if article.source_url.present? %>
              <a href="<%= article.source_url %>" target="_blank" class="text-sm text-indigo-600 dark:text-indigo-400 hover:underline"><%= article.source.presence || article.source_url %></a>
            <% else %>
              <p class="text-sm theme-text-primary"><%= article.source.presence || "—" %></p>
            <% end %>
            <% if article.source_id.present? %>
              <p class="text-xs font-mono theme-text-secondary"><%= article.source_id %></p>
            <% end %>
          </div>

          <!-- Column 3: Context & Feedback -->
          <div>
            <p class="text-sm font-medium theme-text-secondary">Context</p>
            <p class="text-sm theme-text-primary mb-3"><%= article.context.presence || "—" %></p>

            <% if article.feedback.present? %>
              <p class="text-sm font-medium theme-text-secondary">Feedback</p>
              <p class="text-sm theme-text-primary mb-3"><%= article.feedback %></p>
            <% end %>

            <% if article.model.present? %>
              <p class="text-sm font-medium theme-text-secondary">Model</p>
              <p class="text-sm theme-text-primary mb-3"><%= article.model %></p>
            <% end %>

            <% if article.process.present? %>
              <p class="text-sm font-medium theme-text-secondary">Process</p>
              <p class="text-sm theme-text-primary mb-3"><%= article.process %></p>
            <% end %>

            <% if article.process_notes.present? %>
              <p class="text-sm font-medium theme-text-secondary">Process Notes</p>
              <p class="text-sm theme-text-primary mb-3"><%= article.process_notes %></p>
            <% end %>

            <p class="text-xs theme-text-secondary mt-2">Created: <%= article.created_at&.strftime("%b %d, %Y %l:%M %p") || "—" %></p>
            <p class="text-xs theme-text-secondary">Updated: <%= article.updated_at&.strftime("%b %d, %Y %l:%M %p") || "—" %></p>
          </div>

          <!-- Column 4: Actions -->
          <div class="flex flex-col items-end space-y-2">
            <a href="<%= edit_article_path(article) %>" class="inline-flex items-center px-4 py-2 border theme-border-primary rounded-lg text-sm font-medium theme-text-primary hover:theme-bg-tertiary transition-colors">
              Edit / Feedback
              <svg class="ml-2 h-4 w-4 theme-text-secondary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.066 2.573c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.573 1.066c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.066-2.573c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
              </svg>
            </a>
            <a href="#" class="inline-flex items-center px-4 py-2 bg-indigo-600 hover:bg-indigo-700 text-white rounded-lg text-sm font-medium transition-colors shadow">
              Write Post
              <svg class="ml-2 h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
              </svg>
            </a>
          </div>
        </div>

        <!-- JSON Data Fields -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-3 mt-4 pt-4 border-t theme-border-primary">
          <% { "Teams" => article.teams_json, "People" => article.people_json, "Scores" => article.scores_json, "Records" => article.records_json, "Key Stats" => article.key_stats_json, "Quotes" => article.quotes_json }.each do |label, data| %>
            <div>
              <p class="text-xs font-semibold theme-text-secondary uppercase tracking-wide mb-1"><%= label %></p>
              <% if data.present? %>
                <pre class="text-xs theme-text-primary bg-gray-50 dark:bg-gray-800 rounded-lg p-3 overflow-x-auto max-h-40 overflow-y-auto border theme-border-primary"><%= JSON.pretty_generate(data) %></pre>
              <% else %>
                <pre class="text-xs theme-text-secondary italic bg-gray-50 dark:bg-gray-800 rounded-lg p-3 border theme-border-primary">—</pre>
              <% end %>
            </div>
          <% end %>
        </div>

        <!-- Image Viewer -->
        <div class="mt-4 pt-4 border-t theme-border-primary flex flex-wrap gap-6 items-start">
          <!-- Selected Image -->
          <div class="shrink-0">
            <p class="text-xs font-semibold theme-text-secondary uppercase tracking-wide mb-1">Image</p>
            <% if article.image_selected.present? %>
              <div class="rounded-lg border-2 border-indigo-500 overflow-hidden">
                <img src="<%= article.image_selected %>" alt="Selected" class="h-28 object-contain" loading="lazy">
              </div>
            <% else %>
              <div class="h-28 w-28 rounded-lg border theme-border-primary flex items-center justify-center">
                <span class="text-xs theme-text-secondary italic">None</span>
              </div>
            <% end %>
          </div>

          <!-- Options -->
          <div class="ml-auto">
            <% if article.image_options.present? && article.image_options.is_a?(Array) && article.image_options.any? %>
              <p class="text-xs font-semibold theme-text-secondary uppercase tracking-wide mb-1 text-right">Options (<%= article.image_options.length %>)</p>
              <div class="flex flex-wrap gap-2 justify-end">
                <% article.image_options.each_with_index do |img_url, idx| %>
                  <div class="relative group">
                    <div class="rounded-lg border overflow-hidden <%= img_url == article.image_selected ? 'border-2 border-indigo-500 ring-2 ring-indigo-500/30' : 'theme-border-primary' %>">
                      <img src="<%= img_url %>" alt="Image <%= idx + 1 %>" class="h-20 w-20 object-cover" loading="lazy">
                    </div>
                    <% if img_url == article.image_selected %>
                      <div class="absolute top-1 right-1 bg-indigo-600 rounded-full w-5 h-5 flex items-center justify-center shadow">
                        <svg class="w-3 h-3 text-white" fill="none" stroke="currentColor" stroke-width="3" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
                      </div>
                    <% else %>
                      <%= button_to select_image_article_path(article), method: :patch, params: { image_url: img_url }, form: { class: "absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity" } do %>
                        <div class="absolute inset-0 bg-black/50 rounded-lg flex items-center justify-center cursor-pointer">
                          <span class="text-white text-xs font-semibold">Select</span>
                        </div>
                      <% end %>
                    <% end %>
                  </div>
                <% end %>
              </div>
            <% else %>
              <p class="text-xs font-semibold theme-text-secondary uppercase tracking-wide mb-1 text-right">Options</p>
              <p class="text-xs theme-text-secondary italic text-right">No images available</p>
            <% end %>
          </div>
        </div>

        <!-- Feedback Row -->
        <div class="flex flex-wrap items-center gap-6 mt-4 pt-4 border-t theme-border-primary">
          <!-- Article Interesting -->
          <div class="flex items-center gap-2">
            <p class="text-sm font-medium theme-text-secondary">Article Interesting</p>
            <div class="inline-flex rounded-lg border theme-border-primary overflow-hidden">
              <%= button_to feedback_article_path(article), method: :patch, params: { field: "article_good", value: "false" }, class: "px-3 py-0.5 text-sm leading-tight transition-colors #{article.article_good == false ? 'bg-red-100 dark:bg-red-900' : 'hover:theme-bg-tertiary'}", form: { class: "inline" } do %>
                👎
              <% end %>
              <%= button_to feedback_article_path(article), method: :patch, params: { field: "article_good", value: "true" }, class: "px-3 py-0.5 text-sm leading-tight border-l theme-border-primary transition-colors #{article.article_good == true ? 'bg-green-100 dark:bg-green-900' : 'hover:theme-bg-tertiary'}", form: { class: "inline" } do %>
                👍
              <% end %>
            </div>
          </div>

          <!-- Athlete Identified -->
          <div class="flex items-center gap-2">
            <p class="text-sm font-medium theme-text-secondary">Athlete Identified</p>
            <div class="inline-flex rounded-lg border theme-border-primary overflow-hidden">
              <%= button_to feedback_article_path(article), method: :patch, params: { field: "person_identified", value: "false" }, class: "px-3 py-0.5 text-sm leading-tight transition-colors #{article.person_identified == false ? 'bg-red-100 dark:bg-red-900' : 'hover:theme-bg-tertiary'}", form: { class: "inline" } do %>
                👎
              <% end %>
              <%= button_to feedback_article_path(article), method: :patch, params: { field: "person_identified", value: "true" }, class: "px-3 py-0.5 text-sm leading-tight border-l theme-border-primary transition-colors #{article.person_identified == true ? 'bg-green-100 dark:bg-green-900' : 'hover:theme-bg-tertiary'}", form: { class: "inline" } do %>
                👍
              <% end %>
            </div>
          </div>

          <!-- Disposition Coherent -->
          <div class="flex items-center gap-2">
            <p class="text-sm font-medium theme-text-secondary">Disposition Coherent</p>
            <div class="inline-flex rounded-lg border theme-border-primary overflow-hidden">
              <%= button_to feedback_article_path(article), method: :patch, params: { field: "disposition_coherent", value: "false" }, class: "px-3 py-0.5 text-sm leading-tight transition-colors #{article.disposition_coherent == false ? 'bg-red-100 dark:bg-red-900' : 'hover:theme-bg-tertiary'}", form: { class: "inline" } do %>
                👎
              <% end %>
              <%= button_to feedback_article_path(article), method: :patch, params: { field: "disposition_coherent", value: "true" }, class: "px-3 py-0.5 text-sm leading-tight border-l theme-border-primary transition-colors #{article.disposition_coherent == true ? 'bg-green-100 dark:bg-green-900' : 'hover:theme-bg-tertiary'}", form: { class: "inline" } do %>
                👍
              <% end %>
            </div>
          </div>
        </div>
      </div>
    <% end %>

    <% if @articles.empty? %>
      <div class="theme-bg-primary shadow-lg rounded-xl border theme-border-primary p-8 text-center">
        <p class="theme-text-secondary text-sm">No articles yet. <a href="<%= new_article_path %>" class="text-indigo-600 dark:text-indigo-400 hover:underline">Create one</a>.</p>
      </div>
    <% end %>
  </div>
  </div>
</div>
