<!-- Page Title -->
<div class="text-center mb-12">
  <h2 class="text-4xl font-bold text-gray-900 dark:text-white mb-4">Team Performance Rankings</h2>
  <p class="text-lg text-gray-600 dark:text-gray-400 max-w-3xl mx-auto">
    Comprehensive analysis of team strengths across key performance metrics
  </p>
</div>
    <!-- Rankings Grid -->
    <div class="grid grid-cols-1 xl:grid-cols-3 gap-8">
      
      <!-- QB Passing Rankings -->
      <div class="bg-white dark:bg-gray-800 rounded-xl shadow-lg overflow-hidden border border-gray-200 dark:border-gray-700 hover:shadow-xl transition-shadow duration-300">
        <div class="bg-gradient-to-r from-blue-500 to-blue-600 px-6 py-4">
          <div class="flex items-center space-x-3">
            <span class="text-2xl">🖐️</span>
            <div>
              <h3 class="text-lg font-bold text-white">QB Passing</h3>
              <p class="text-blue-100 text-sm">Quarterback Performance</p>
            </div>
          </div>
        </div>
        <div class="p-6">
          <div class="space-y-3">
            <% @qb_passing_rankings.each_with_index do |ranking, index| %>
              <div class="p-3 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors cursor-pointer" onclick="window.location.href='<%= team_show_path(ranking[:team]&.slug) %>'">
                <div class="flex items-center justify-between mb-2">
                  <div class="flex items-center space-x-3">
                    <div class="flex items-center justify-center w-8 h-8 rounded-full bg-blue-100 dark:bg-blue-900 text-blue-600 dark:text-blue-300 font-bold text-sm">
                      <%= index + 1 %>
                    </div>
                    <div class="flex items-center space-x-2">
                      <span class="text-xl"><%= ranking[:team]&.emoji %></span>
                      <span class="font-semibold text-gray-900 dark:text-white"><%= ranking[:team]&.name %></span>
                    </div>
                  </div>
                  <div class="text-right">
                    <div class="text-lg font-bold text-gray-900 dark:text-white">
                      <%= ranking[:qb].passing_grade_x %>
                    </div>
                  </div>
                </div>
                <div class="text-xs text-gray-500 dark:text-gray-400 pl-11">
                  <%= ranking[:qb]&.player %> <i>Pass: <%= ranking[:qb]&.passing_grade_x %></i>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>

      <!-- Receiver Core Rankings -->
      <div class="bg-white dark:bg-gray-800 rounded-xl shadow-lg overflow-hidden border border-gray-200 dark:border-gray-700 hover:shadow-xl transition-shadow duration-300">
        <div class="bg-gradient-to-r from-green-500 to-green-600 px-6 py-4">
          <div class="flex items-center space-x-3">
            <span class="text-2xl">🤲</span>
            <div>
              <h3 class="text-lg font-bold text-white">Receiver Core</h3>
              <p class="text-green-100 text-sm">Top 3 Receivers Combined</p>
            </div>
          </div>
        </div>
        <div class="p-6">
          <div class="space-y-3">
            <% @receiving_rankings.each_with_index do |ranking, index| %>
              <div class="p-3 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors cursor-pointer" onclick="window.location.href='<%= team_show_path(ranking[:team]&.slug) %>'">
                <div class="flex items-center justify-between mb-2">
                  <div class="flex items-center space-x-3">
                    <div class="flex items-center justify-center w-8 h-8 rounded-full bg-green-100 dark:bg-green-900 text-green-600 dark:text-green-300 font-bold text-sm">
                      <%= index + 1 %>
                    </div>
                    <div class="flex items-center space-x-2">
                      <span class="text-xl"><%= ranking[:team]&.emoji %></span>
                      <span class="font-semibold text-gray-900 dark:text-white"><%= ranking[:team]&.name %></span>
                    </div>
                  </div>
                  <div class="text-right">
                    <div class="text-lg font-bold text-gray-900 dark:text-white">
                      <%= number_with_precision(ranking[:receiver_score], precision: 1) %>
                    </div>
                  </div>
                </div>
                <div class="text-xs text-gray-500 dark:text-gray-400 pl-11">
                  <%= ranking[:receivers].map { |receiver| "#{receiver.last_name} #{receiver.grades_pass_route_print || 'N/A'}" }.join(', ') %>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>

      <!-- O-Line Pass Block Rankings -->
      <div class="bg-white dark:bg-gray-800 rounded-xl shadow-lg overflow-hidden border border-gray-200 dark:border-gray-700 hover:shadow-xl transition-shadow duration-300">
        <div class="bg-gradient-to-r from-purple-500 to-purple-600 px-6 py-4">
          <div class="flex items-center space-x-3">
            <span class="text-2xl">🛡️</span>
            <div>
              <h3 class="text-lg font-bold text-white">Pass Block Core</h3>
              <p class="text-purple-100 text-sm">O-Line Protection</p>
            </div>
          </div>
        </div>
        <div class="p-6">
          <div class="space-y-3">
            <% @pass_block_rankings.each_with_index do |ranking, index| %>
              <div class="p-3 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors cursor-pointer" onclick="window.location.href='<%= team_show_path(ranking[:team]&.slug) %>'">
                <div class="flex items-center justify-between mb-2">
                  <div class="flex items-center space-x-3">
                    <div class="flex items-center justify-center w-8 h-8 rounded-full bg-purple-100 dark:bg-purple-900 text-purple-600 dark:text-purple-300 font-bold text-sm">
                      <%= index + 1 %>
                    </div>
                    <div class="flex items-center space-x-2">
                      <span class="text-xl"><%= ranking[:team]&.emoji %></span>
                      <span class="font-semibold text-gray-900 dark:text-white"><%= ranking[:team]&.name %></span>
                    </div>
                  </div>
                  <div class="text-right">
                    <div class="text-lg font-bold text-gray-900 dark:text-white">
                      <%= number_with_precision(ranking[:pass_block_score], precision: 1) %>
                    </div>
                  </div>
                </div>
                <div class="text-xs text-gray-500 dark:text-gray-400 pl-11">
                  <% if ranking[:qb].left_handed? %>
                    <%= raw ranking[:roster].left_tackle_player.last_name %> (<%= raw ranking[:roster].left_tackle_player.pass_block_grade_x %>)
                  <% else %> 
                    <strong><%= raw ranking[:roster].left_tackle_player.last_name %> (<%= raw ranking[:roster].left_tackle_player.pass_block_grade_x %>)</strong>
                  <% end %>
                  <%= raw ranking[:roster].left_guard_player.last_name %> (<%= raw ranking[:roster].left_guard_player.pass_block_grade_x %>)
                  <strong><%= raw ranking[:roster].center_player.last_name %> (<%= raw ranking[:roster].center_player.pass_block_grade_x %>)</strong>
                    <%= raw ranking[:roster].right_guard_player.last_name %> (<%= raw ranking[:roster].right_guard_player.pass_block_grade_x %>)
                  <% if ranking[:qb].left_handed? %>
                    <strong><%= raw ranking[:roster].right_tackle_player.last_name %> <%= raw ranking[:roster].right_tackle_player.pass_block_grade_x %></strong>
                  <% else %>
                    <%= raw ranking[:roster].right_tackle_player.last_name %> (<%= raw ranking[:roster].right_tackle_player.pass_block_grade_x %>)
                  <% end %>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>

    </div>

    <!-- Second Row: Pass Rush, Coverage -->
    <div class="grid grid-cols-1 xl:grid-cols-2 gap-8 mb-12 mt-12">
      
      <!-- Pass Rush Core Rankings -->
      <div class="bg-white dark:bg-gray-800 rounded-xl shadow-lg overflow-hidden border border-gray-200 dark:border-gray-700 hover:shadow-xl transition-shadow duration-300">
        <div class="bg-gradient-to-r from-red-500 to-red-600 px-6 py-4">
          <div class="flex items-center space-x-3">
            <span class="text-2xl">💥</span>
            <div>
              <h3 class="text-lg font-bold text-white">Pass Rush Core</h3>
              <p class="text-red-100 text-sm">D-Line Pressure</p>
            </div>
          </div>
        </div>
        <div class="p-6">
          <div class="space-y-3">
            <% @pass_rush_rankings.each_with_index do |ranking, index| %>
              <div class="p-3 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors cursor-pointer" onclick="window.location.href='<%= team_show_path(ranking[:team]&.slug) %>'">
                <div class="flex items-center justify-between mb-2">
                  <div class="flex items-center space-x-3">
                    <div class="flex items-center justify-center w-8 h-8 rounded-full bg-red-100 dark:bg-red-900 text-red-600 dark:text-red-300 font-bold text-sm">
                      <%= index + 1 %>
                    </div>
                    <div class="flex items-center space-x-2">
                      <span class="text-xl"><%= ranking[:team]&.emoji %></span>
                      <span class="font-semibold text-gray-900 dark:text-white"><%= ranking[:team]&.name %></span>
                    </div>
                  </div>
                  <div class="text-right">
                    <div class="text-lg font-bold text-gray-900 dark:text-white">
                      <%= number_with_precision(ranking[:score], precision: 1) %>
                    </div>
                  </div>
                </div>
                <div class="text-xs text-gray-500 dark:text-gray-400 pl-11">
                  <strong><%= raw ranking[:roster].edge1_player.last_name %> (<%= raw ranking[:roster].edge1_player.pass_rush_grade_x %>)</strong>
                  <%= raw ranking[:roster].dl1_player.last_name %> (<%= raw ranking[:roster].dl1_player.pass_rush_grade_x %>)
                  <%= raw ranking[:roster].dl2_player.last_name %> (<%= raw ranking[:roster].dl2_player.pass_rush_grade_x %>)
                  <%= raw ranking[:roster].dl3_player.last_name %> (<%= raw ranking[:roster].dl3_player.pass_rush_grade_x %>)
                  <strong><%= raw ranking[:roster].edge2_player.last_name %> (<%= raw ranking[:roster].edge2_player.pass_rush_grade_x %>)</strong>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>

      <!-- Coverage Core Rankings -->
      <div class="bg-white dark:bg-gray-800 rounded-xl shadow-lg overflow-hidden border border-gray-200 dark:border-gray-700 hover:shadow-xl transition-shadow duration-300">
        <div class="bg-gradient-to-r from-indigo-500 to-indigo-600 px-6 py-4">
          <div class="flex items-center space-x-3">
            <span class="text-2xl">🔒</span>
            <div>
              <h3 class="text-lg font-bold text-white">Coverage Core</h3>
              <p class="text-indigo-100 text-sm">Secondary Coverage</p>
            </div>
          </div>
        </div>
        <div class="p-6">
          <div class="space-y-3">
            <% @coverage_rankings.each_with_index do |ranking, index| %>
              <div class="p-3 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors cursor-pointer" onclick="window.location.href='<%= team_show_path(ranking[:team]&.slug) %>'">
                <div class="flex items-center justify-between mb-2">
                  <div class="flex items-center space-x-3">
                    <div class="flex items-center justify-center w-8 h-8 rounded-full bg-indigo-100 dark:bg-indigo-900 text-indigo-600 dark:text-indigo-300 font-bold text-sm">
                      <%= index + 1 %>
                    </div>
                    <div class="flex items-center space-x-2">
                      <span class="text-xl"><%= ranking[:team]&.emoji %></span>
                      <span class="font-semibold text-gray-900 dark:text-white"><%= ranking[:team]&.name %></span>
                    </div>
                  </div>
                  <div class="text-right">
                    <div class="text-lg font-bold text-gray-900 dark:text-white">
                      <%= number_with_precision(ranking[:score], precision: 1) %>
                    </div>
                  </div>
                </div>
                <div class="text-xs text-gray-500 dark:text-gray-400 pl-11">
                  <strong><%= raw ranking[:roster].cb1_player.last_name %> (<%= raw ranking[:roster].cb1_player.coverage_grade_x %>)</strong>
                  <%= raw ranking[:roster].cb3_player.last_name %> (<%= raw ranking[:roster].cb3_player.coverage_grade_x %>)
                  <%= raw ranking[:roster].safety1_player.last_name %> (<%= raw ranking[:roster].safety1_player.coverage_grade_x %>)
                  <%= raw ranking[:roster].safety2_player.last_name %> (<%= raw ranking[:roster].safety2_player.coverage_grade_x %>)
                  <strong><%= raw ranking[:roster].cb2_player.last_name %> (<%= raw ranking[:roster].cb2_player.coverage_grade_x %>)</strong>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>

    </div>

    <!-- Third Row: Rushing, Run Block, Run Defense -->
    <div class="grid grid-cols-1 xl:grid-cols-3 gap-8 mb-12 mt-12">
      
      <!-- Rushing Core Rankings -->
      <div class="bg-white dark:bg-gray-800 rounded-xl shadow-lg overflow-hidden border border-gray-200 dark:border-gray-700 hover:shadow-xl transition-shadow duration-300">
        <div class="bg-gradient-to-r from-orange-500 to-orange-600 px-6 py-4">
          <div class="flex items-center space-x-3">
            <span class="text-2xl">🏃</span>
            <div>
              <h3 class="text-lg font-bold text-white">Rushing Core</h3>
              <p class="text-orange-100 text-sm">Top 2 Rushers</p>
            </div>
          </div>
        </div>
        <div class="p-6">
          <div class="space-y-3">
            <% @rushing_rankings.each_with_index do |ranking, index| %>
              <div class="p-3 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors cursor-pointer" onclick="window.location.href='<%= team_show_path(ranking[:team]&.slug) %>'">
                <div class="flex items-center justify-between mb-2">
                  <div class="flex items-center space-x-3">
                    <div class="flex items-center justify-center w-8 h-8 rounded-full bg-orange-100 dark:bg-orange-900 text-orange-600 dark:text-orange-300 font-bold text-sm">
                      <%= index + 1 %>
                    </div>
                    <div class="flex items-center space-x-2">
                      <span class="text-xl"><%= ranking[:team]&.emoji %></span>
                      <span class="font-semibold text-gray-900 dark:text-white"><%= ranking[:team]&.name %></span>
                    </div>
                  </div>
                  <div class="text-right">
                    <div class="text-lg font-bold text-gray-900 dark:text-white">
                      <%= number_with_precision(ranking[:score], precision: 1) %>
                    </div>
                  </div>
                </div>
                <div class="text-xs text-gray-500 dark:text-gray-400 pl-11">
                  <%= ranking[:rushers].map { |rusher| "#{rusher.last_name} #{rusher.rush_grade_x || 'N/A'}" }.join(', ') %>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>

      <!-- O-Line Run Block Core Rankings -->
      <div class="bg-white dark:bg-gray-800 rounded-xl shadow-lg overflow-hidden border border-gray-200 dark:border-gray-700 hover:shadow-xl transition-shadow duration-300">
        <div class="bg-gradient-to-r from-amber-500 to-amber-600 px-6 py-4">
          <div class="flex items-center space-x-3">
            <span class="text-2xl">🏃</span>
            <div>
              <h3 class="text-lg font-bold text-white">Run Block Core</h3>
              <p class="text-amber-100 text-sm">O-Line Run Blocking</p>
            </div>
          </div>
        </div>
        <div class="p-6">
          <div class="space-y-3">
            <% @run_block_rankings.each_with_index do |ranking, index| %>
              <div class="p-3 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors cursor-pointer" onclick="window.location.href='<%= team_show_path(ranking[:team]&.slug) %>'">
                <div class="flex items-center justify-between mb-2">
                  <div class="flex items-center space-x-3">
                    <div class="flex items-center justify-center w-8 h-8 rounded-full bg-amber-100 dark:bg-amber-900 text-amber-600 dark:text-amber-300 font-bold text-sm">
                      <%= index + 1 %>
                    </div>
                    <div class="flex items-center space-x-2">
                      <span class="text-xl"><%= ranking[:team]&.emoji %></span>
                      <span class="font-semibold text-gray-900 dark:text-white"><%= ranking[:team]&.name %></span>
                    </div>
                  </div>
                  <div class="text-right">
                    <div class="text-lg font-bold text-gray-900 dark:text-white">
                      <%= number_with_precision(ranking[:score], precision: 1) %>
                    </div>
                  </div>
                </div>
                <div class="text-xs text-gray-500 dark:text-gray-400 pl-11">
                  <%= raw ranking[:roster].left_tackle_player.last_name %> (<%= raw ranking[:roster].left_tackle_player.run_block_grade_x %>)
                  <strong><%= raw ranking[:roster].left_guard_player.last_name %> (<%= raw ranking[:roster].left_guard_player.run_block_grade_x %>)</strong>
                  <strong><%= raw ranking[:roster].center_player.last_name %> (<%= raw ranking[:roster].center_player.run_block_grade_x %>)</strong>
                  <strong><%= raw ranking[:roster].right_guard_player.last_name %> (<%= raw ranking[:roster].right_guard_player.run_block_grade_x %>)</strong>
                  <%= raw ranking[:roster].right_tackle_player.last_name %> (<%= raw ranking[:roster].right_tackle_player.run_block_grade_x %>)
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>

      <!-- Run Defense Core Rankings -->
      <div class="bg-white dark:bg-gray-800 rounded-xl shadow-lg overflow-hidden border border-gray-200 dark:border-gray-700 hover:shadow-xl transition-shadow duration-300">
        <div class="bg-gradient-to-r from-green-500 to-green-600 px-6 py-4">
          <div class="flex items-center space-x-3">
            <span class="text-2xl">🛡️</span>
            <div>
              <h3 class="text-lg font-bold text-white">Run Defense Core</h3>
              <p class="text-green-100 text-sm">Defensive Run Stop</p>
            </div>
          </div>
        </div>
        <div class="p-6">
          <div class="space-y-3">
            <% @run_defense_rankings.each_with_index do |ranking, index| %>
              <div class="p-3 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors cursor-pointer" onclick="window.location.href='<%= team_show_path(ranking[:team]&.slug) %>'">
                <div class="flex items-center justify-between mb-2">
                  <div class="flex items-center space-x-3">
                    <div class="flex items-center justify-center w-8 h-8 rounded-full bg-green-100 dark:bg-green-900 text-green-600 dark:text-green-300 font-bold text-sm">
                      <%= index + 1 %>
                    </div>
                    <div class="flex items-center space-x-2">
                      <span class="text-xl"><%= ranking[:team]&.emoji %></span>
                      <span class="font-semibold text-gray-900 dark:text-white"><%= ranking[:team]&.name %></span>
                    </div>
                  </div>
                  <div class="text-right">
                    <div class="text-lg font-bold text-gray-900 dark:text-white">
                      <%= number_with_precision(ranking[:score], precision: 1) %>
                    </div>
                  </div>
                </div>
                <div class="text-xs text-gray-500 dark:text-gray-400 pl-11">

                <%= raw ranking[:roster].edge1_player.last_name %> (<%= raw ranking[:roster].edge1_player.rush_defense_grade_x %>)
                <strong><%= raw ranking[:roster].dl2_player.last_name %> (<%= raw ranking[:roster].dl2_player.rush_defense_grade_x %>)</strong>
                <strong><%= raw ranking[:roster].dl1_player.last_name %> (<%= raw ranking[:roster].dl1_player.rush_defense_grade_x %>)</strong>
                <strong><%= raw ranking[:roster].dl3_player.last_name %> (<%= raw ranking[:roster].dl3_player.rush_defense_grade_x %>)</strong>
                <%= raw ranking[:roster].edge2_player.last_name %> (<%= raw ranking[:roster].edge2_player.rush_defense_grade_x %>)
                <br><br>
                <%= raw ranking[:roster].cb3_player.last_name %> (<%= raw ranking[:roster].cb3_player.rush_defense_grade_x %>)
                <strong><%= raw ranking[:roster].safety1_player.last_name %> (<%= raw ranking[:roster].safety1_player.rush_defense_grade_x %>)</strong>
                <strong><%= raw ranking[:roster].lb1_player.last_name %> (<%= raw ranking[:roster].lb1_player.rush_defense_grade_x %>)</strong>
                <strong><%= raw ranking[:roster].lb2_player.last_name %> (<%= raw ranking[:roster].lb2_player.rush_defense_grade_x %>)</strong>
                <%= raw ranking[:roster].safety2_player.last_name %> (<%= raw ranking[:roster].safety2_player.rush_defense_grade_x %>)
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>

    </div>

    <!-- Fourth Row: Coaching Rankings -->
    <div class="grid grid-cols-1 xl:grid-cols-3 gap-8 mb-12 mt-12">
      
      <!-- Play Caller Rankings -->
      <div class="bg-white dark:bg-gray-800 rounded-xl shadow-lg overflow-hidden border border-gray-200 dark:border-gray-700 hover:shadow-xl transition-shadow duration-300">
        <div class="bg-gradient-to-r from-blue-500 to-blue-600 px-6 py-4">
          <div class="flex items-center space-x-3">
            <span class="text-2xl">🏈</span>
            <div>
              <h3 class="text-lg font-bold text-white">Play Caller Rankings</h3>
              <p class="text-blue-100 text-sm">Offensive Play Calling</p>
            </div>
          </div>
        </div>
        <div class="p-6">
          <div class="space-y-3">
            <% @play_caller_rankings.each_with_index do |rank, index| %>
              <div class="p-3 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors">
                <div class="flex items-center justify-between mb-2">
                  <div class="flex items-center space-x-3">
                    <div class="flex items-center justify-center w-8 h-8 rounded-full bg-blue-100 dark:bg-blue-900 text-blue-600 dark:text-blue-300 font-bold text-sm">
                      <%= index + 1 %>
                    </div>
                    <div class="flex items-center space-x-2">
                      <span class="text-xl"><%= rank.team&.emoji %></span>
                      <span class="font-semibold text-gray-900 dark:text-white"><%= rank.team&.name %></span>
                    </div>
                  </div>
                  <div class="text-right">
                    <div class="text-lg font-bold text-gray-900 dark:text-white">
                      <%= rank&.offensive_play_caller_rank %>
                    </div>
                  </div>
                </div>
                <div class="text-xs text-gray-500 dark:text-gray-400 pl-11">
                  <%= rank&.full_name %>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>

      <!-- Pace of Play Rankings -->
      <div class="bg-white dark:bg-gray-800 rounded-xl shadow-lg overflow-hidden border border-gray-200 dark:border-gray-700 hover:shadow-xl transition-shadow duration-300">
        <div class="bg-gradient-to-r from-green-500 to-green-600 px-6 py-4">
          <div class="flex items-center space-x-3">
            <span class="text-2xl">⚡</span>
            <div>
              <h3 class="text-lg font-bold text-white">Pace of Play Rankings</h3>
              <p class="text-green-100 text-sm">Offensive Tempo</p>
            </div>
          </div>
        </div>
        <div class="p-6">
          <div class="space-y-3">
            <% @pace_rankings.each_with_index do |coach, index| %>
              <div class="p-3 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors">
                <div class="flex items-center justify-between mb-2">
                  <div class="flex items-center space-x-3">
                    <div class="flex items-center justify-center w-8 h-8 rounded-full bg-green-100 dark:bg-green-900 text-green-600 dark:text-green-300 font-bold text-sm">
                      <%= index + 1 %>
                    </div>
                    <div class="flex items-center space-x-2">
                      <span class="text-xl"><%= coach.team&.emoji %></span>
                      <span class="font-semibold text-gray-900 dark:text-white"><%= coach.team&.name %></span>
                    </div>
                  </div>
                  <div class="text-right">
                    <div class="text-lg font-bold text-gray-900 dark:text-white">
                      <%= coach&.pace_of_play_rank %>
                    </div>
                  </div>
                </div>
                <div class="text-xs text-gray-500 dark:text-gray-400 pl-11">
                  <%= coach&.full_name %>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>

      <!-- Run Heavy Rankings -->
      <div class="bg-white dark:bg-gray-800 rounded-xl shadow-lg overflow-hidden border border-gray-200 dark:border-gray-700 hover:shadow-xl transition-shadow duration-300">
        <div class="bg-gradient-to-r from-orange-500 to-orange-600 px-6 py-4">
          <div class="flex items-center space-x-3">
            <span class="text-2xl">🏃</span>
            <div>
              <h3 class="text-lg font-bold text-white">Run Heavy Rankings</h3>
              <p class="text-orange-100 text-sm">Run-First Offense</p>
            </div>
          </div>
        </div>
        <div class="p-6">
          <div class="space-y-3">
            <% @run_heavy_rankings.each_with_index do |coach, index| %>
              <div class="p-3 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors">
                <div class="flex items-center justify-between mb-2">
                  <div class="flex items-center space-x-3">
                    <div class="flex items-center justify-center w-8 h-8 rounded-full bg-orange-100 dark:bg-orange-900 text-orange-600 dark:text-orange-300 font-bold text-sm">
                      <%= index + 1 %>
                    </div>
                    <div class="flex items-center space-x-2">
                      <span class="text-xl"><%= coach.team&.emoji %></span>
                      <span class="font-semibold text-gray-900 dark:text-white"><%= coach.team&.name %></span>
                    </div>
                  </div>
                  <div class="text-right">
                    <div class="text-lg font-bold text-gray-900 dark:text-white">
                      <%= coach&.run_heavy_rank %>
                    </div>
                  </div>
                </div>
                <div class="text-xs text-gray-500 dark:text-gray-400 pl-11">
                  <%= coach&.full_name %>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>

    </div>

    <!-- Footer -->
    <footer class="mt-16 text-center">
      <div class="text-sm text-gray-500 dark:text-gray-400">
        <p>Data updated for the <%= params[:year] %> NFL season</p>
        <p class="mt-2">
          <a href="<%= matchups_week1_path(@year) %>" class="text-blue-600 dark:text-blue-400 hover:text-blue-800 dark:hover:text-blue-300 underline">
            View Week 1 Matchups
          </a>
          <span class="mx-2">•</span>
          <a href="<%= teams_seasons_path %>" class="text-blue-600 dark:text-blue-400 hover:text-blue-800 dark:hover:text-blue-300 underline">
            View Team Rosters
          </a>
        </p>
      </div>
    </footer> 