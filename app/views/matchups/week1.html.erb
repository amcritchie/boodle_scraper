<!DOCTYPE html>
<html>
<head>
  <title>Matchups - Season 2025, Week 1</title>
  <style>
    table {
      width: 100%;
      border-collapse: collapse;
    }
    th, td {
      border: 1px solid #ddd;
      padding: 8px;
      text-align: left;
    }
    th {
      background-color: #f4f4f4;
    }
  </style>
</head>
<body>
  <h1>Matchups for Season 2025, Week 1</h1>
  <table>
    <thead>
      <tr>
        <th rowspan="2" width="60">Team</th>
        <th colspan="3">Offense</th>
        <th rowspan="2" width="60">Team Defense</th>
        <th colspan="2">Defense</th>
      </tr>
      <tr>
        <th>Passer Score</th>
        <th>Receiver Score</th>
        <th>Pass Block Score</th>
        <th>Sack Score</th>
        <th>Coverage Score</th>
      </tr>
    </thead>    
    <tbody>
      <% @matchups.each do |matchup| %>
        <tr>
          <td><%= matchup.team.emoji %> <%= matchup.team.slug.upcase %></td>
          <td><b><%= matchup.passer_tier %> <%= matchup.passer_score %></b> <%= matchup.qb.last_name rescue "-" %></td>
          <td><b><%= matchup.reciever_tier %><%= matchup.reciever_score %></b> <%= matchup.reciever_factors.join(', ') %></td>
          <td><b><%= matchup.pass_block_tier %><%= matchup.pass_block_score %></b> <%= matchup.pass_block_factors.join(', ') %></td>
          <td><%= matchup.team_defense.emoji %> <%= matchup.team.slug.upcase %></td>
          <td><b><%= matchup.sack_tier %><%= matchup.sack_score %></b> <%= matchup.sack_factors.join(', ') %></td>
          <td><b><%= matchup.coverage_tier %><%= matchup.coverage_score %></b> <%= matchup.coverage_factors.first(3).join(', ') %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</body>
</html>
